#!/bin/sh -eu

cd "$(dirname "$0")/.."

nix shell \
    --ignore-environment \
    --keep LANG \
    --keep LOCALE_ARCHIVE \
    .#org2gfm \
    .#nixpkgs.ansifilter \
    .#nixpkgs.coreutils \
    .#nixpkgs.git \
    .#nixpkgs.gnugrep \
    .#nixpkgs.gnutar \
    .#nixpkgs.gzip \
    .#nixpkgs.jq \
    .#nixpkgs.nixfmt \
    .#nixpkgs.tree \
    --command \
    org2gfm \
    --path-bin \
    --exclude internal \
    --exclude example \
    --nix "$(command -v nix)" \
    --evaluate \
    "$@"
