#!/bin/sh -eu

cd "$(dirname "$0")/.."

nix run \
    --ignore-environment \
    --file nix \
    nix-project-org2gfm \
    pkgs.coreutils \
    --command \
    org2gfm \
    --ignore 'scaffold\|support' \
    --nix "$(command -v nix)" \
    --evaluate \
    "$@"
