#!/bin/sh -eu

cd "$(dirname "$0")/.."

nix run \
    --ignore-environment \
    --file nix \
    nix-project-exe \
    --command \
    nix-project \
    --source-dir nix/sources \
    --nix "$(command -v nix)" \
    init-update \
    "$@"
